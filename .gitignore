# Dependencies (dev-time only; consumers never install anything)
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/

# Coverage & temp
coverage/
.tmp/

# Agent-harness worktrees (docs/swarm/ task implementations, ephemeral)
.claude/

# NOTE: dist/ is intentionally NOT ignored — release artifacts are committed (ADR-0007).
