/**
 * @module components/dv-dropdown.locale
 * `dv-dropdown`'s own locale bundle (ADR-0019) — co-located, not centralized, so wiring another
 * component never touches this file. `en` matches the component's pre-existing hardcoded
 * default verbatim.
 */
export default {
  en: {
    label: 'Menu',
  },
  tr: {
    label: 'Menü',
  },
};
