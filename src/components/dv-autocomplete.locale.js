/**
 * @module components/dv-autocomplete.locale
 * `dv-autocomplete`'s own locale bundle (ADR-0019) — co-located, not centralized, so wiring
 * another component never touches this file. `en` matches the component's pre-existing
 * hardcoded default verbatim.
 */
export default {
  en: {
    label: 'Search',
  },
  tr: {
    label: 'Ara',
  },
};
