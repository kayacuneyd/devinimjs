# `dv-counter`

Canonical `component()` migration example. It accepts numeric `data-start` and `data-step`
props, renders an accessible stepper and emits `dv:change` with `{ count }`.

The source is intentionally concise: [../../src/components/dv-counter.js](../../src/components/dv-counter.js).
