# `dv-search`

AI-first search-field example. It accepts `data-query`, `data-label` and `data-placeholder`,
then emits `dv:query` with `{ query }` for each user edit or clear action.

The source is [../../src/components/dv-search.js](../../src/components/dv-search.js).
