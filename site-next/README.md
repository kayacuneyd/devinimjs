# DevinimJS public site (new)

Clean-start redesign workspace for the public DevinimJS site. The existing `site/` directory is
intentionally untouched and remains the legacy reference.

Preview with the repository's dependency-free static server:

```bash
node scripts/serve.mjs --port 8080
```

Then open `http://localhost:8080/site-next/`.
