declare namespace _default {
    namespace en {
        let label: string;
        let jumpLabel: string;
        let previousLabel: string;
        let nextLabel: string;
        let previousPageLabel: string;
        let nextPageLabel: string;
        let pageLabel: string;
        let jumpAriaLabel: string;
        let goLabel: string;
    }
    namespace tr {
        let label_1: string;
        export { label_1 as label };
        let jumpLabel_1: string;
        export { jumpLabel_1 as jumpLabel };
        let previousLabel_1: string;
        export { previousLabel_1 as previousLabel };
        let nextLabel_1: string;
        export { nextLabel_1 as nextLabel };
        let previousPageLabel_1: string;
        export { previousPageLabel_1 as previousPageLabel };
        let nextPageLabel_1: string;
        export { nextPageLabel_1 as nextPageLabel };
        let pageLabel_1: string;
        export { pageLabel_1 as pageLabel };
        let jumpAriaLabel_1: string;
        export { jumpAriaLabel_1 as jumpAriaLabel };
        let goLabel_1: string;
        export { goLabel_1 as goLabel };
    }
}
export default _default;
